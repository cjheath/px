source "https://rubygems.org"

# Pins Jekyll and its plugins to what GitHub Pages actually runs in production,
# so a local build matches the deployed site as closely as possible.
gem "github-pages", group: :jekyll_plugins

# github-pages' webrick dependency was dropped from Ruby's standard library
# in Ruby 3; harmless to include even where it's already present.
gem "webrick"
